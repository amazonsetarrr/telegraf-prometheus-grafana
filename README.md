/etc/telegraf/
│── telegraf.conf            # global config (agent + outputs)
/etc/telegraf/telegraf.d/
│── cisco-router.conf        # inputs only
│── cisco-switch.conf
│── cisco-wlc.conf
│── f5-lb.conf
│── cp-firewall.conf
│── netflow.conf
│── ping.conf
